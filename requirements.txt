fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
openai==1.59.6
lyricsgenius==3.0.1
yt-dlp==2024.12.13
pydantic==2.10.4
beautifulsoup4==4.12.3
requests==2.32.3
numpy>=1.26,<3   # 자동 오프셋(onset 분석)용



# 일본어 -> 한글 독음 변환
#   fugashi(+unidic): 형태소 분석으로 조사 は->わ 등 문법·문맥 한자 읽기 정확
#   pykakasi: 보조(잔여 한자 히라가나화)
fugashi==1.5.2
unidic-lite==1.0.8
pykakasi==2.3.0

# (선택) 로컬 강제정렬(stable-ts/torch)은 용량이 커서 기본 배포에서 제외.
#   설치 안 해도 동작합니다(LRCLIB 동기가사 + Whisper 근사 정렬로 폴백).
#   더 높은 보컬 싱크 정확도를 원하면:  pip install -r requirements-align.txt
